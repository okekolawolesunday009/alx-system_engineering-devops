#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints a simple string
 * Return: 0 if program executes, else prints a non-zero number
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
